# ── Datos sensibles / binarios grandes ──
# Excel legacy del antiguo hotel · ahora vive en archive/, también ignorado.
OFERTA BODAS Y EVENTOS 2011 HOTEL REINA CRISTINA.xls
archive/

# ── Base de datos generada ──
database/*.db
database/*.db-journal
database/*.db-wal
database/*.db-shm

# ── Logs ──
logs/
*.log

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.venv/
venv/

# ── Editores / IDEs ──
.vscode/
.idea/
*.swp
*.swo

# ── Sistema operativo ──
.DS_Store
Thumbs.db
desktop.ini

# ── Credenciales y secretos ──
.env
.env.local
*.key
*.pem
secrets.json
.local-credentials.txt
mockups/screenshot-*.png
.test/

# ── npm + Playwright (v5.8 · B2 tests E2E) ──
node_modules/
playwright-report/
test-results/
.playwright/

# ── Claude Code launch config (local-only) ──
.claude/
